# Fixora Monorepo

India-first two-sided freelance marketplace scaffold with:
- Flutter mobile app (`mobile/`)
- Node.js + Express backend (`backend/`)
- PostgreSQL migration SQL (`backend/src/db/migrations/`)

See `docs/architecture.md` for API and escrow flow details.
